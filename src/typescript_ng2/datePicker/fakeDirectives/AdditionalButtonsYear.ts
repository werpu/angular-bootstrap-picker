import {Directive} from "@angular/core";

@Directive({selector: 'additional-buttons-year'})
export class AdditionalButtonsYear {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}