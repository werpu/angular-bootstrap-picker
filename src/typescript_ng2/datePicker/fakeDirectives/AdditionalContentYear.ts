import {Directive} from "@angular/core";

@Directive({selector: 'additional-content-year'})
export class AdditionalContentYear {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}