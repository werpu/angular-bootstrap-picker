import {Directive} from "@angular/core";

@Directive({selector: 'additional-buttons-month'})
export class AdditionalButtonsMonth {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}