import {Directive} from "@angular/core";

@Directive({selector: 'additional-buttons-date'})
export class AdditionalButtonsDate {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}