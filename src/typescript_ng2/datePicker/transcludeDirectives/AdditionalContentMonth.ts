import {Directive} from "@angular/core";

@Directive({selector: 'additional-content-month'})
export class AdditionalContentMonth {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}