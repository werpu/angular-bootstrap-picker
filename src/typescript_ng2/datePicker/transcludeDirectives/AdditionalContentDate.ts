import {Directive} from "@angular/core";

@Directive({selector: 'additional-content-date'})
export class AdditionalContentDate {
    // No behavior
    // The only purpose is to "declare" the tag in Angular2
}